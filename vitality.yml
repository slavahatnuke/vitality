
install vitality :
  if.show: false # by default
  if: which vitality
  else: npm install -g vitality

#
#install vitality same:
#  if: which vitality
#  else:
#    - echo 'Start installing vitality'
#    - npm install -g vitality
#    - echo 'vitality was installed -g'
#
#remote hello: ## repository https://github.com/slavahatnuke/vitalities
#  if: vitality @/vitality.yml
#
#rm tmp:
#  if.not: ls | grep tmp
#  else: rm -rf tmp