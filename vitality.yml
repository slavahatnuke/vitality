
lessc.is.installed:
  title:  lessc is installed
  test:   which lessc
  build:  npm -g install less

lessc.is.installed2:
  title:  lessc is installed2
  test:   which lessc
  build:  npm -g install less
